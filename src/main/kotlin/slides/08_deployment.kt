package ws.slides

import ws.kpres.PresentationBuilder
import ws.utils.slideTitle

fun PresentationBuilder.qovery() {
    slide { slideTitle("Docker...") }
    slide { slideTitle("Qovery to the rescue!") }
}
